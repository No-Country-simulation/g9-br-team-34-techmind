# Servicio de inferencia TechMind - dependencias fijadas por version exacta.
#
# Las versiones estan pinneadas (== y no >=) a proposito: una imagen Docker que
# resuelve versiones distintas en cada build no es reproducible, y un fallo en
# produccion que no se reproduce en local es tiempo perdido que en un hackathon
# no hay.

# --- API ---
fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4
pydantic-settings==2.15.0

# --- Ciencia de Datos ---
# 1.8.0 (no 1.6.0): los artefactos que entrega Ciencia de Datos en
# data-science/API/ se serializaron con scikit-learn 1.8.0 y numpy 2.x; cargarlos
# con una version anterior rompe (o degrada silenciosamente) la deserializacion.
scikit-learn==1.8.0
scipy==1.15.2
pandas==2.2.3
numpy==2.2.1
joblib==1.4.2

# --- Preprocesado en espanol (lematizacion) ---
# La limpieza del notebook usa spaCy: sin el mismo lematizador que se aplico al
# entrenar, las predicciones en produccion se degradarian. El modelo de espanol
# se instala como paquete desde el release oficial de spaCy-models (su version
# 3.8.0 exige spaCy >=3.8.0,<3.9.0, de ahi el pin en 3.8.x).
spacy==3.8.15
es_core_news_sm @ https://github.com/explosion/spacy-models/releases/download/es_core_news_sm-3.8.0/es_core_news_sm-3.8.0-py3-none-any.whl

# --- Integracion OCI (descarga del modelo desde Object Storage) ---
oci==2.142.0
