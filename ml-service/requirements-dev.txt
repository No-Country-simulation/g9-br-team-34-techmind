# Dependencias de desarrollo y pruebas.
#
# Van en un archivo aparte de requirements.txt para que NO entren en la imagen
# de produccion: un runner de pruebas dentro del contenedor desplegado es codigo
# que nunca se ejecuta y superficie de ataque que si existe.

-r requirements.txt

pytest==9.1.1
httpx==0.28.1
ruff==0.16.3
