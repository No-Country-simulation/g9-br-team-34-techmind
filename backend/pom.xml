<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.0</version>
        <relativePath/>
    </parent>

    <groupId>com.api.techmind-g9-team34</groupId>
    <artifactId>api-techmind</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>api-techmind</name>
    <description>API para integracion con modelo de Data Science</description>

    <properties>
        <java.version>17</java.version>

        <!--
            Parche de seguridad del Tomcat embebido, agregado por DevOps.

            Spring Boot 3.3.0 trae Tomcat 10.1.24, que arrastra cuatro
            vulnerabilidades CRITICAS con parche publicado:

              CVE-2025-24813  ejecucion remota de codigo por subida de archivos
              CVE-2026-41293  cabeceras HTTP/2 sin validar
              CVE-2026-43512  omision de autenticacion digest
              CVE-2026-43515  autorizacion incorrecta

            Actualizar Spring Boot NO las resuelve: hasta la 3.5.3 siguen
            fijando Tomcat 10.1.42. Se comprobo consultando el BOM de cada
            version.

            Sobrescribir esta unica propiedad es lo mas acotado que se puede
            hacer: mantiene la version de Spring Boot que eligio el equipo y
            solo mueve Tomcat dentro de su misma linea 10.1.x, que es
            compatible a nivel de API.

            Cuando el equipo suba a un Spring Boot que ya traiga 10.1.55 o
            superior, esta linea se puede borrar. El escaneo de Trivy en CI
            avisa si se queda corta.
        -->
        <tomcat.version>10.1.55</tomcat.version>
    </properties>

    <dependencies>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!--
            Agregado por DevOps. Da el endpoint /actuator/health, que es lo que
            consulta el HEALTHCHECK de la imagen y, a traves de el, la condicion
            `service_healthy` del compose y la espera del despliegue.

            Antes se usaba /v3/api-docs para no tocar el pom, pero esa es una
            ruta funcional: el dia que SecurityConfig la proteja, el contenedor
            quedaria unhealthy pese a funcionar y el CD revertiria cada
            despliegue. Un endpoint de salud dedicado no tiene ese acoplamiento.

            Solo se expone `health`; el resto de endpoints de actuator quedan
            apagados (ver application.properties).
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.5.0</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Dependencias para integracion con modelo de ciencia de datos -->
        <!--
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>

        <dependency>
            <groupId>ai.djl</groupId>
            <artifactId>api</artifactId>
            <version>0.24.0</version>
        </dependency>
        -->

        <!-- Oracle Cloud Infrastructure SDK -->
        <dependency>
            <groupId>com.oracle.oci.sdk</groupId>
            <artifactId>oci-java-sdk-common</artifactId>
            <version>3.89.1</version>
        </dependency>

        <dependency>
            <groupId>com.oracle.oci.sdk</groupId>
            <artifactId>oci-java-sdk-objectstorage</artifactId>
            <version>3.89.1</version>
        </dependency>

        <dependency>
            <groupId>com.oracle.oci.sdk</groupId>
            <artifactId>oci-java-sdk-common-httpclient-jersey3</artifactId>
            <version>3.89.1</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>